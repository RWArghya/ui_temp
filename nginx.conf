server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # ── Gzip ──────────────────────────────────────
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml image/svg+xml;
    gzip_min_length 256;

    # ── SPA fallback ──────────────────────────────
    # All non-file routes fall back to index.html so
    # React Router can handle client-side routing.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # ── API reverse proxy ─────────────────────────
    # Proxies /api/* requests to the Go backend.
    #
    # Uses Docker's internal DNS (127.0.0.11) and a
    # variable so Nginx doesn't crash on startup when
    # the backend container isn't running yet.
    #
    # ${API_UPSTREAM} is substituted at container start (see Dockerfile).
    # It must match the service/container name the backend publishes on the
    # shared h2s-net network — the H2S Go API is h2s-api:8080, while the name
    # "backend" currently belongs to a different project.
    location /api/v1 {
        resolver 127.0.0.11 valid=30s ipv6=off;
        set $backend_upstream http://${API_UPSTREAM};

        proxy_pass         $backend_upstream$request_uri;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # ── Static asset caching ─────────────────────
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
